import { User } from "../model/User"
import { performPurchase } from "../service/testando"



describe("Testando compras de usuario ",()=>{
    
    test("Retorna verdade para saldo maior que o valor de compra",()=>{
       
        const valor = true
        
        expect(valor).toBe(true)
    })

})